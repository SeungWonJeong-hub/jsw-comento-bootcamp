# 2차 업무 — Unit Test 구성 및 2D → 3D 변환

**달 사진 두 장으로 지형의 높낮이를 복원한다.** 착륙 지점을 고르려면 어디가
평평한지 알아야 하는데, 사진에는 색만 있고 높낮이가 없다.

```
사진 2장  →  시차 d  →  깊이 맵 Z = f·B/d  →  3D 포인트 클라우드
             ↑ 두 사진에서 밀린 픽셀 수      ↑ 그것을 3D 로 바꾼다
```

| | 값 |
|---|---:|
| 대상 | 티코 크레이터 (높낮이 차 **5.376 km**) |
| 고도 오차 중앙값 | **0.0302 km** (기복의 0.56%) |
| 90 퍼센타일 | 0.0816 km |
| RMSE | 0.0828 km |
| 덮은 셀 | **76.1%** |
| 포인트 클라우드 | 311,550 점 |
| — 사진 한 쌍만 쓰면 | 0.0346 km · 62.7% |

여기서 **높낮이 차**는 그 지형에서 가장 높은 곳과 가장 낮은 곳의 차이다.
오차가 큰지 작은지는 그 값과 견주어야 뜻이 생긴다. 아래 본문에서는
지형학에서 쓰는 말 그대로 **기복**이라고 적는다.

정답은 **LRO LOLA 레이저 고도계로 실제 측정된 고도**다. 근사로 만든 기준이
아니다. 시차 1픽셀이 0.45 km 를 바꾸는 촬영 조건에서
오차가 0.0302 km 이므로 **부화소까지 맞춘 셈**이다.

**수렴각이 다른 네 쌍을 합쳐 얻은 값이다.** 한 쌍만 쓰면 0.0346 km 에
62.7% 를 덮는데, 합치면 정확도와 덮는 범위가 **함께** 좋아진다(7-7절).

**두 장은 달 광도함수와 센서 모델을 거쳐 만든다.** 시선 방향이 밝기에 들어가고
광자 잡음·광학 흐림·양자화가 얹힌다(8절). 램버트 반사에 잡음 없이 돌리면 같은
설정에서 오차가 0.0359 km 로 나오는데, 그 숫자는 실제 영상에 옮겨 말할 수 없다.

**실제로 찍힌 사진으로도 확인했다.** 1971년 아폴로 15호가 필름에 찍은 두 장을
같은 파이프라인에 넣어 복원한 기복이 2.74 km 로, 같은 지역 LOLA 의
2.72 km 와 1.01 배 안에서 맞는다(11절).

- 고도 모델: LRO LOLA 0.118 km/화소 (USGS Astrogeology, 공개 자료)
- 실제 사진: 아폴로 15호 매핑 카메라 AS15-M-1000 / 1001 (ASU 아폴로 영상 아카이브)
- 테스트: pytest **79개** 통과 (고도 모델이 없으면 3개 skip)

---

## 1. 실행 방법

```bash
pip install -r requirements.txt

py -3 tools/get_moon_dtm.py                 # 고도 모델 한 구역 (약 2 MB, 최초 1회)
py -3 tools/save_test_report.py             # Unit Test 79개 → outputs/pytest_report.txt
py -3 run_3d_experiment.py data/moon/tycho_118m.tif   # 실험 → outputs/
py -3 tools/build_report.py [출력폴더]       # 발표자료 + 발표 대본

py -3 tools/get_apollo_pair.py              # 실제로 찍힌 사진 두 장 (28 MB)
py -3 run_apollo_stereo.py                  # 그 두 장으로 고도 복원
```

고도 모델이 없어도 `py -3 run_3d_experiment.py` 만 치면 합성 지형으로 돌아간다.
발표자료(.pptx)와 대본(.txt)은 저장소에 커밋하지 않는다(`report/` 는 `.gitignore`).

## 2. 촬영을 어떻게 세웠는가

궤도에서 찍은 원본 영상으로 곧장 스테레오를 돌리려면 카메라 자세 커널과 지도
투영을 다루는 전용 도구 체계가 필요하다. 그 준비만으로 며칠이 간다. 대신
**이미 레이저로 측정된 고도 모델에서 출발**한다.

고도를 3D 점으로 펴고, 자세를 아는 카메라 두 대로 다시 찍은 뒤, 그 두 장으로
고도를 복원해 원래 값과 비교한다.

> **기하는 실제 달이고 영상은 렌더링이다.** 밝기는 달 광도함수(Lunar-Lambert)
> 로 만들고 광자 잡음·광학 흐림·8비트 양자화를 거치므로 시선 방향과 잡음이
> 실제처럼 들어가지만, 실제로 찍은 사진은 아니다. 이 실험이 검증하는 것은
> **촬영 기하와 정합**이다. 8절에 그 모델이 결과를 얼마나 바꾸는지 재 놓았다.

**두 카메라는 같은 지점을 바라본다(수렴 촬영).** 실제 궤도 스테레오도 나란히
두 대를 놓는 것이 아니라, 지나가며 앞뒤로 기울여 같은 지역을 두 번 찍는다.

```
R1, t1 / R2, t2 = 두 촬영 지점의 자세
① 상대 자세   R = R2 · R1ᵀ,   t = t2 − R · t1
② 베이스라인  B = 2 · H · tan(수렴각 / 2)
③ 정렬·정합   stereoRectify(R, t) → StereoSGBM → 시차 d
④ 깊이        Z = f · B / d
⑤ 3D 좌표     X = (u−cx)·Z/f,  Y = (v−cy)·Z/f
```

| 기호 | 뜻 | 값 |
|---|---|---:|
| H | 촬영 고도 | 59.2 km |
| 수렴각 | 두 시선이 벌어진 각 | 15° |
| B | 베이스라인 | 15.6 km |
| f | 초점거리 | 500 px |
| — | 시차 1픽셀이 바꾸는 Z | 0.45 km |
| SNR | 최대 밝기에서의 신호 대 잡음비 | 100 |
| — | 태양 고도 | 25° |

**초점거리를 고도에서 유도한다.** `f = H / 화소 크기`로 두면 영상 화소와 지형
화소가 1:1 이 된다. 고도를 상수로 박으면 고도 모델이 바뀔 때(합성 0.005 km,
LOLA 0.118 km) 시차가 화면을 넘거나 몇 픽셀로 쪼그라든다.

## 3. 결과

`outputs/00_overview.png` 에 왼쪽 영상부터 복원 깊이까지 한 줄로 있다.

| | 값 |
|---|---:|
| Z 오차 중앙값 | **0.0338 km** (시차 0.08 px) |
| RMSE | 0.0718 km |
| 시차 0.25 px (0.112 km) 이내 | **96.1%** |
| 시차 0.50 px (0.225 km) 이내 | 99.7% |
| 시차 1.00 px (0.45 km) 이내 | 100.0% |
| 값이 나온 픽셀 | 67.9% |

**허용오차를 km 로 못 박지 않고 시차 몇 픽셀인지로 잡았다.** 시차 1픽셀이
바꾸는 깊이가 이 촬영 기하의 분해능 한계이므로, 그보다 훨씬 작은 값을 기준으로
삼으면 알고리즘이 아니라 운을 재게 된다. 촬영 조건이 바뀌어도 뜻이 같다는
장점도 있다.

> 처음에는 0.005·0.01·0.02·0.05 km 를 썼다가 "0.02 km 이내 16.5%" 같은 값을 보고했다. 0.02 km 는
> 분해능의 1/22 이라 원리적으로 도달할 수 없는 기준인데, 성능이 나쁜 것처럼
> 읽힌다. 지표는 장비의 한계에 맞춰야 한다.

RMSE 가 0.0718 km 인데, 광도 잔차 관문을 걸기 전에는 0.1 km 였다. RMSE 는 크게
틀린 소수의 화소가 끌어올리는 값이므로, 그 화소들을 정답 없이 골라내 버리는
것만으로 크게 준다(9-3절). 중앙값과 함께 봐야 한다.

## 4. 수렴각 — 정밀도와 덮는 범위가 맞바뀐다

두 시선이 벌어진 각이 이 실험의 중심 파라미터다. 각이 크면 삼각형이 뚱뚱해져
거리가 정밀해지지만, 두 영상이 서로 달라 보여 같은 지점을 찾기 어려워진다.

| 수렴각 | 베이스라인 | 시차 1px = Z | 값이 나온 픽셀 | Z 오차 중앙값 |
|---:|---:|---:|---:|---:|
| 10° | 10.4 km | 0.677 km | 76.6% | 0.0583 km |
| **15°** | **15.6 km** | **0.45 km** | **68.0%** | **0.0333 km** |
| 20° | 20.9 km | 0.336 km | 59.6% | 0.0361 km |
| 30° | 31.7 km | 0.221 km | 43.3% | 0.0279 km |

**10°에서 30°로 벌리면 오차가 0.0583 → 0.0279 km 로 줄지만,
값이 나온 픽셀이 76.6 → 43.3% 로 떨어진다.** 한 쌍만 쓴다면
여기서 타협해야 한다.

**그런데 타협하지 않아도 된다.** 각도마다 덮는 곳이 다르므로 여러 각도를 합치면
둘 다 얻는다. 7-7절이 그 측정이다.

`outputs/01_tradeoff.png` 의 왼쪽 그래프에서 두 선이 반대로 가는 것이 이것이다.

## 5. 정합 블록 크기

| 블록 | 값이 나온 픽셀 | Z 오차 중앙값 |
|---:|---:|---:|
| 3 | 67.3% | 0.0336 km |
| 5 | 67.5% | 0.0324 km |
| 7 | 67.6% | 0.0324 km |
| 9 | 67.8% | 0.0329 km |
| **11** | **67.9%** | **0.0338 km** |
| 15 | 68.2% | 0.0358 km |

**잡음이 있으면 큰 블록이 정확하다.** 블록이 작으면 창 안의 무늬가 적어 잡음
한 점이 정합을 흔들고, 크면 기울어진 면에서 깊이가 뭉개진다. 그 균형점이 11 이다.

> **이 결론은 처음과 반대다.** 잡음 없는 렌더링에서는 블록 3~5 가 이겼고 README
> 에도 "작은 블록이 정확하다" 라고 적혀 있었다. 센서 모델을 넣자 최적점이
> 옮겨 갔다 — 작은 블록이 잡음을 무늬로 착각하기 때문이다. **손으로 고른
> 파라미터는 촬영 조건이 바뀌면 조용히 틀린 값으로 남는다.** 10절이 이 문제에
> 대한 답이다.

**차이가 크지 않다는 점도 함께 봐야 한다.** 3에서 15까지 오차가
0.0336 → 0.0358 km 로 -1% 움직이는데, 수렴각을 바꾸면 두 배 이상
움직인다. **파라미터보다 촬영 기하가 결과를 지배한다.**

## 6. Unit Test 설계

과제 예시의 테스트(p.14)는 출력 크기와 자료형만 확인한다. 그것만으로는 수식이
틀려도 통과한다. **손으로 답을 낼 수 있는 조건**을 만들어 숫자까지 대조했다.

| 갈래 | 예시 | 검증 방식 |
|---|---|---|
| **해석해** | 평지를 수직으로 보면 깊이 = 촬영 고도 | 1e-6 이내 |
| | 기울기 tan(t) 사면의 법선 | 1e-9 이내 |
| | 태양이 머리 위면 밝기 = albedo | 1e-9 이내 |
| **불변식** | `B = 2·H·tan(수렴각/2)` | 상대오차 1e-12 |
| | `p_j = R·p_i + t` 가 실제로 성립 | 1e-12 이내 |
| | B와 d를 함께 2배 하면 Z 불변 | 스케일 모호성 |
| **왕복** | 깊이 맵 → 3D → 고도가 원래 값과 같은가 | 0.002 km 이내 |
| **전체** | 렌더링→정렬→정합→깊이 를 한 번에 | 시차 0.5 px 이내 |
| **회귀** | 렌더링한 두 장이 `f·B/d` 에서 겹치는가 | 실제 결함을 잡은 테스트 |
| | 수직 시점 두 대가 오른쪽을 뒤집지 않는가 | 두 번째 결함 |
| | 지형이 카메라보다 높으면 멈추는가 | 세 번째 결함 |
| | 무늬가 없는 곳을 걸러내는가 | 네 번째 결함 |
| **경계** | 화소 크기 0, 수렴각 90°, 크기 불일치 | 예외 처리 |

**독립적인 경우는 `@pytest.mark.parametrize` 로 나눈다.** 잘못된 수렴각 네 가지,
잘못된 화소 크기 두 가지가 그렇다. 한 번에 루프를 돌면 첫 실패에서 멈춰
나머지가 실행되지 않고 어느 값에서 깨졌는지도 이름에 안 남는다.

## 7. 검토에서 찾아 고친 것

파이프라인을 다섯 갈래로 훑었다 — 기하 항등식, 수치 안정성, 데이터 흐름,
재현성, 교란 실험. **일곱 건이 나왔고 전부 고쳤다.**

> 이 절의 표들은 **8절의 촬영 모델을 넣기 전에** 잰 값이다. 그때는 램버트
> 반사에 잡음이 없었으므로 절대값이 3절과 다르다. 각 항목이 무엇을 얼마나
> 바꿨는지를 보여 주는 기록이라 당시 값 그대로 둔다.

### 7-1. 렌더링이 시차를 밀어내고 있었다

처음 실행에서 Z 오차가 **1.245 km** 나왔다. 분해능이 0.45 km/px 인데 2.8 px 이라
잡음치고는 크고, 무엇보다 **오차가 한쪽으로 치우쳐** 있었다(부호 있는 오차 평균
−1.101 km).

| 확인한 것 | 결과 |
|---|---|
| 정렬 창 보정이 두 카메라의 주점을 어긋내나 | 아님 — 같은 양만큼 옮긴다 |
| `Z = f·B/d` 가 실제 투영과 맞나 | **정확히 일치** |
| 정렬 좌표계에서 직접 렌더링하면 | **오프셋 0.0 px** → 기하와 기준 깊이는 옳다 |
| `alpha` 를 바꾸면 오프셋이 | 2.0 / 2.6 / 3.6 → **초점거리에 비례** (각도 0.3°) |
| 점 번짐(splat)을 끄면 | +2.6 → **+0.6 px** |
| 화소마다 광선을 쏘면 | **−0.10 px** |

원인은 렌더러였다. 점을 화소에 던져 그리면서 위치를 **반올림**하는데, 그 오차가
표면 기울기 방향으로 쏠린다. 수렴 촬영의 두 카메라는 서로 반대로 기울어 있으므로
좌우가 반대로 쏠리고, 그 차이가 그대로 시차 치우침이 된다. 점 번짐은 깊이 버퍼에
3×3 최소값 필터를 거는 셈이라 표면을 카메라 쪽으로 당겨 이를 키웠다.

번짐은 **빈 화소를 메우는 데만** 쓰도록 바꾸고, 흩뿌리기 자체를 **화소마다
광선을 쏘아 지형과 만나는 점을 이중선형으로 찾는 방식**으로 교체했다.
**Z 오차 1.245 km → 0.066 km.**

### 7-2. 수직 시점 두 대의 오른쪽이 뒤집혔다

카메라 자세를 만들 때 "위쪽"을 수직(0,0,1)으로 뒀다. 아래를 내려다보는 카메라는
시선이 거의 −Z 라 그 기준과 나란해지고, 외적이 0 에 가까워져 영상의 오른쪽이
시선의 미세한 수평 성분으로 정해진다. **두 카메라의 상대 회전이 15°가 아니라
180°로 나왔다.** 수평 기준(북쪽)을 주어 고쳤다.

### 7-3. 깊이 탐색 구간을 잘못 잡았다

`고도 ± 기복`으로 잡았는데, 정렬된 카메라는 수렴각의 절반만큼 기울어 있어
**지형이 넓으면 Z 가 화면을 가로질러 크게 변한다.** 티코(75 km 폭)에서 Z 가
±5 km 흔들려 옳은 시차가 구간 밖으로 잘려 나갔다. 지형 경계 상자의 여덟
꼭짓점을 정렬 카메라로 옮겨 실제 Z 범위를 구하도록 고쳤다.

### 7-4. 광선이 수렴하지 않은 채 값을 내고 있었다

복원한 점이 실제로 고도장 위에 있는지 재 보니 화소의 **10%가 잔차 0.001 km 를 넘었고
최악은 0.3 km** 였다. 반복 횟수를 다섯 배로 늘려도 줄지 않았다 — 수렴이 느린
것이 아니라 **아예 수렴하지 않는 것**이었다. 지형 격자 밖으로 나간 광선이
가장자리 값으로 고정되어 진동한다.

반복 뒤 t 를 한 번 더 갱신하고, **마지막 갱신 폭이 화소 크기의 1/100 을 넘으면
값을 내지 않는다.** 잔차 최악 **0.3 km → 0.00004 km**, 0.001 km 초과 0%.

### 7-5. 무늬가 없는 곳에서 아무 값이나 내고 있었다

크게 틀린 화소(1 px 초과)의 평균 밝기가 **62** 로 전체 평균 **114** 보다 훨씬
어두웠다 — 크레이터 안쪽 그늘이다. 국소 대비(7×7 표준편차) 하한을 재 봤다.

| 대비 하한 | 값이 나온 화소 | Z 오차 중앙값 | 1 px 초과 |
|---:|---:|---:|---:|
| 0 | 72.0% | 0.0661 km | 0.99% |
| **2** | **69.4%** | **0.0649 km** | **0.69%** |
| 4 | 63.0% | 0.0633 km | 0.75% |
| 8 | 47.2% | 0.0609 km | 0.94% |

하한 2 는 화소를 2.6 %p 만 내주고 **크게 틀린 화소를 3분의 1 줄인다.**
`uniquenessRatio` 는 30 까지 올려도 지렛대가 되지 못했다(21 → 16.5%).

### 7-6. 시차가 정수 부근에 몰려 있었다 (픽셀 락킹)

시차 소수부를 세어 보니 정수 부근(±0.1 px)에 **49.3%** 가 몰려 있었다. 균일하면
20% 여야 한다. SGBM 부화소 보간의 알려진 성질이고, 이것이 깊이를 **+0.043 px
(약 0.019 km)** 치우치게 만들었다 — 당시 전체 오차 0.065 km 의 3분의 1 이다.

시차는 가로 방향이므로 **가로로만 늘려 정합한 뒤 되돌린다.**

| 배율 | 값이 나온 화소 | Z 오차 중앙값 | 정수 부근 | 시차 편향 |
|---:|---:|---:|---:|---:|
| 1× | 69.4% | 0.0649 km | 49.3% | +0.043 px |
| **2×** | **69.3%** | **0.0346 km** | **35.9%** | **+0.024 px** |
| 4× | 69.6% | 0.043 km | 40.9% | +0.028 px |

**2배에서 오차가 거의 절반이 되고 화소 손실은 없다.** 4배는 오히려 나빠진다 —
보간이 없는 정보를 만들어 내지는 못한다.

### 7-7. 여러 수렴각을 합치면 트레이드오프가 사라진다

4절에서 "정밀도와 덮는 범위를 동시에 얻을 수 없다"고 적었는데, **한 쌍만 쓸
때의 이야기**였다. 각도가 다르면 정합에 성공하는 곳도 다르다. 정밀한 각도는
좁고 촘촘하게, 넓은 각도는 넓고 성기게 덮는다.

각도마다 정렬 창의 크기가 달라 깊이 맵끼리는 화소 단위로 겹칠 수 없다. **지형
격자**가 모든 각도가 공유하는 유일한 좌표계이고, 만들려는 산출물 자체가 "격자
위의 고도"이므로 거기서 합친다. 가중치는 **1/분해능²** — 시차 1픽셀이 바꾸는
깊이가 그 각도의 정밀도이고, 오차가 거기 비례한다면 역분산 가중이 그 값이 된다.

| | 덮은 셀 | 오차 중앙값 | 90 퍼센타일 | RMSE |
|---|---:|---:|---:|---:|
| 10° 단독 | 70.9% | 0.0611 km | 0.143 km | 0.0993 km |
| 15° 단독 | 62.7% | 0.0346 km | 0.0923 km | 0.13 km |
| 20° 단독 | 54.5% | 0.038 km | 0.0856 km | 0.168 km |
| 30° 단독 | 39.1% | **0.0293 km** | 0.0718 km | 0.212 km |
| **네 각도 융합** | **76.1%** | **0.0302 km** | 0.0816 km | **0.0828 km** |

**어느 단독 각도보다 넓게 덮으면서, 30° 단독을 빼면 가장 정확하다.** 30°는
0.0293 km 로 정확하지만 39.1% 밖에 못 덮는다. RMSE 는 융합이 가장
좋다 — 크게 틀린 값이 걸러지기 때문이다.

**각도끼리 어긋나는 셀을 버리는 것이 신뢰도 역할을 한다.** 정답을 보지 않고
"믿을 수 없는 곳"을 가려낼 수 있는 단서다. 서로 다른 기하에서 본 값이 다르면
둘 중 하나 이상이 틀린 것이다. 두 각도 이상이 값을 낸 셀이
66.0%, 세 각도 이상이 53.2% 다.

이 단서가 **실제로 오차를 예측하는지**는 오랫동안 확인하지 않은 채 쓰고 있었다.
9절에서 잰다.

### 7-8. 다른 지역에서도 같은 결과가 나오는가

한 크레이터에 맞춰진 결과가 아닌지 확인하려고, 같은 설정 그대로 코페르니쿠스
크레이터(20.0°N, 3.6°W, 기복 5.659 km)에서 다시 돌렸다.

| | 티코 | 코페르니쿠스 |
|---|---:|---:|
| 기복 | 5.376 km | 5.659 km |
| Z 오차 중앙값 (한 쌍) | 0.0408 km | 0.058 km |
| 시차 단위 | 0.09 px | 0.13 px |
| 값이 나온 픽셀 | 70.5% | 61.1% |
| 융합 오차 중앙값 | 0.0376 km | 0.0445 km |
| 융합이 덮은 셀 | 76.6% | 63.0% |
| 영상에서 정해진 대비 하한 | 1.48 | 1.55 |

> 이 표는 두 곳 모두 **상수 알베도**로 돌린 값이다. 실제 표면 무늬를 쓴 사진이
> 티코에만 있어서, 한쪽만 그것을 쓰면 지역끼리 견주는 뜻이 사라진다. 그래서
> 3절의 머리 숫자(무늬를 쓴 값)보다 조금 나쁘다.

**정확도는 같은 수준이고 덮는 범위가 다르다.** 코페르니쿠스는 위도 20°N 이라
태양 고도가 달라 그늘이 더 넓다. 파라미터를 다시 고르지 않고 그대로 돌린
결과다 — 대비 하한만 영상의 잡음을 보고 스스로 1.48 에서 1.55 로 옮겨 갔다(10절).

## 8. 촬영 모델 — 실제에 얼마나 가까운가

렌더링 영상으로 잰 성적을 실제 영상에 옮겨 말하려면, 렌더링이 실제와 어디서
다른지를 먼저 알아야 한다. 두 가지를 넣었다.

**① 밝기에 시선 방향이 들어간다.** 램버트 반사는 밝기가 태양 방향에만
의존한다. 그러면 같은 지면 조각이 두 사진에서 **정확히 같은 밝기**로 찍힌다.
수렴각을 아무리 벌려도 밝기는 변하지 않으므로, 정합기는 실제로는 겪지 않을
쉬운 문제를 푸는 셈이 된다. 실제 달 표면은 후방산란이 강해 보는 방향에 따라
밝기가 달라진다. 달 광도함수(Lunar-Lambert, McEwen)로 바꿨다.

```
I = A [ 2 L(g) mu0 / (mu0 + mu) + (1 - L(g)) mu0 ]

  mu0 = cos(입사각)    법선과 태양 방향의 사이각
  mu  = cos(방출각)    법선과 카메라 방향의 사이각   <- 여기서 시점이 들어온다
  g   = 위상각         태양 방향과 카메라 방향의 사이각
  L(g) = 1 + A1 g + A2 g^2 + A3 g^3      (도 단위)
```

L=0 이면 램버트, L=1 이면 Lommel-Seeliger 가 되는 보간식이다. 티코에서 재 보면
두 시점의 밝기가 중앙값 1.5%, 90 퍼센타일 2.8% 만큼 달라진다.

**② 센서를 거친다.** 광학 흐림(PSF) 0.7 px, 광자 잡음, 읽기 잡음,
8비트 양자화. 광자 잡음은 밝기의 제곱근에 비례하므로 **어두운 곳이 상대적으로
더 나쁘다** — 크레이터 그늘에서 정합이 어려운 물리적 이유다. 잡음을 밝기와
무관한 상수로 넣으면 그 사실이 실험에서 사라진다.

| 촬영 모델 | 값이 나온 픽셀 | Z 오차 중앙값 |
|---|---:|---:|
| 램버트 · 상수 알베도 · 무잡음 | 67.7% | 0.0349 km |
| 달 반사 · 상수 알베도 · 무잡음 | 67.4% | 0.0346 km |
| 달 반사 · 실제 무늬 · 무잡음 | 67.5% | 0.0314 km |
| 달 반사 · 실제 무늬 · SNR 400 | 68.1% | 0.0331 km |
| **달 반사 · 실제 무늬 · SNR 100 (채택)** | **67.9%** | **0.0338 km** |
| 달 반사 · 실제 무늬 · SNR 40 | 66.5% | 0.0458 km |

**아래로 갈수록 실제 촬영에 가깝고 그만큼 성적이 나빠진다.** 잡음이 지배적이다 —
반사 모델만 바꾸는 것은 0.000367 km 밖에 안 움직이지만, SNR 을 100 으로
내리면 0.00534 km 가 더해진다. SNR 40 에서는 값이 나온 픽셀까지 무너진다.

**아직 실제로 찍은 사진은 아니다.** 이 표가 말하는 것은 "렌더링의 어느 부분이
결과를 얼마나 좌우하는가" 이지 "실제 영상에서도 이만큼 나온다" 가 아니다.

## 9. 정답 없이 믿을 곳을 가려낸다

실제 운용에는 정답 고도가 없다. 그러면 복원한 값 중 어디를 믿을지 두 영상만
보고 정해야 한다. 세 가지를 넣고, **그것이 실제로 오차를 예측하는지**까지 쟀다.

### 9-1. 좌우 일관성 검사

왼쪽 기준으로 x 가 x−d 에 대응한다면, 오른쪽 기준으로도 x−d 가 x 를 도로
가리켜야 한다. 한쪽에서만 보이는 곳(가림)에서는 이 왕복이 깨진다. 매처는
대응이 없어도 반드시 무언가를 고르므로, 그 사실을 알려 주는 유일한 단서다.

| | 값이 나온 픽셀 | Z 오차 중앙값 | 90 퍼센타일 |
|---|---:|---:|---:|
| 좌우 일관성 없음 | 72.5% | 0.0353 km | 0.112 km |
| **좌우 일관성 적용** | **67.9%** | **0.0338 km** | **0.108 km** |

**효과가 작다.** 다른 관문을 이미 통과한 화소 중 1.7% 만 더 걸러 낸다. SGBM 의
고유성 비율 검사와 얼룩 필터가 이미 상당 부분을 하고 있기 때문이다. 90 퍼센타일이
0.1122 에서 0.1083 km 로 주는 것이 실질적인 이득이다.

### 9-2. 그 신뢰도가 진짜 오차를 예측하는가 — 희소화 곡선

신뢰도가 낮은 순서로 화소를 버려 가며 남은 화소의 평균 오차를 그린다. 같은 일을
**실제 오차 순서로** 하면 가장 빠르게 떨어지는 곡선(오라클)이 나온다. 두 곡선
사이의 넓이가 AUSE 이고, 작을수록 그 신뢰도가 오차를 잘 예측한다는 뜻이다.

여기서 정답을 쓰는 것은 **채점할 때뿐**이다. 신뢰도 자체는 정답 없이 계산된다.

| 신뢰도 | AUSE | 순위 상관 | 크게 틀린 것 AUC | 90% 버렸을 때 평균 오차 |
|---|---:|---:|---:|---:|
| 광도 잔차 | **0.353** | **+0.108** | 0.939 | 0.0631 → 0.0566 km |
| 봉우리 margin | 0.276 | +0.251 | 0.936 | 0.0631 → 0.0316 km |
| 각도 간 편차 (격자) | 0.457 | -0.067 | 0.971 | 0.0435 → 0.0427 km |
| 무작위 (대조군) | 0.609 | -0.001 | 0.501 | 0.0631 → 0.064 km |
| 국소 대비 | 0.931 | -0.154 | 0.255 | 0.0631 → 0.161 km |
| — 오라클 (실제 오차 순) | 0 | — | 1 | → 0.00303 km |

**셋 다 무작위보다 낫지만 오라클과는 멀다.** 광도 잔차가 가장 낫고, 발표자료에서
신뢰도로 쓰던 각도 간 편차는 무작위보다 조금 나은 정도다. **국소 대비는
무작위보다 나쁘다** — 대비가 높은 크레이터 테두리가 오히려 더 크게 틀리기
때문이다. 대비를 신뢰도로 쓰면 안 된다는 뜻이고, 재 보기 전에는 몰랐다.

## 10. 파라미터를 영상에서 정한다

상수로 박은 값은 이 지형에서 훑어 고른 값이다. 지형이나 촬영 조건이 바뀌면 다시
골라야 하는데, 그러면 자동으로 도는 파이프라인이 아니다. 5절의 블록 크기가 실제로
그렇게 뒤집혔다.

| 파라미터 | 예전 | 지금 |
|---|---|---|
| 시차 탐색 범위 | 지형 경계 상자에서 유도 | 그대로 (이미 옳았다) |
| 대비 하한 | 회색조 **2.0** 을 손으로 | **잡음 x 2** = 1.48 |
| 블록 크기 | 손으로 고른 상수 | 아래 참조 |

**대비 하한은 풀렸다.** 영상 한 장에서 잡음 표준편차를 추정하고(Immerkaer) 그
배수로 잡는다. 티코에서 0.74 x 2 = 1.48 가 나오는데, 손으로 고른 2.0 과
거의 같으면서 **영상이 바뀌면 따라간다** — 코페르니쿠스에서는 1.55 가 된다.

**블록 크기는 절반만 풀렸다.** 광도 잔차와 좌우 일관성 통과율은 블록에 대해
단조라서 항상 양 끝을 고른다. 대신 **수렴각이 다른 두 촬영의 결과가 서로 얼마나
일치하는지**를 썼다. 정답을 전혀 쓰지 않으면서 복원의 정확도를 직접 겨눈다.

| 블록 | 일관성 통과 | 광도 잔차 | 15°–30° 불일치 | 재조명 일치 | 정답 대비 Z 오차 |
|---:|---:|---:|---:|---:|---:|
| 3 | 64.2% | 2.23 | 0.0346 km | 0.308 | 0.0336 km |
| 5 | 64.5% | 2.25 | 0.0273 km | 0.334 | 0.0324 km |
| 7 | 64.8% | 2.27 | 0.0267 km | 0.346 | 0.0324 km |
| 9 | 65.3% | 2.28 | 0.0267 km | 0.343 | 0.0329 km |
| 11 | 65.6% | 2.30 | 0.0267 km | 0.344 | 0.0338 km |
| 15 | 65.9% | 2.33 | 0.0267 km | 0.333 | 0.0358 km |
| 21 | 65.2% | 2.35 | 0.0267 km | 0.303 | 0.0388 km |
| 31 | 13.6% | 4.37 | 0.184 km | -0.024 | 0.286 km |

**정답 없이 고르면 블록 21, 정답으로 고르면 11 이다.** 맞았는지보다 잘못
골랐을 때 얼마를 더 내는지가 중요하다 — 0.00609 km,
15% 다.

**왜 어긋나는가.** 블록을 키우면 결과가 매끄러워지고, 그 매끄러움은 두 각도에
**똑같이** 일어난다. 그래서 불일치는 계속 줄어드는데 실제 오차는 뭉개짐 때문에
늘기 시작한다. 다만 블록 31 에서는 둘이 함께 무너지므로, 이 기준이 **완전한
실패는 잡아낸다**. 감쇠가 아니라 붕괴만 잡는 셈이다.

## 11. 실제로 찍힌 사진으로 확인한다

8~10절까지의 결과는 모두 **렌더링한 두 장**에서 나온 것이다. 반사 모델과 센서를
실제 쪽으로 옮겨 놓았지만 사진 자체는 만든 것이다. 그래서 실제로 찍힌 사진을
같은 파이프라인에 넣어 보았다.

### 11-1. 왜 아폴로인가

먼저 Kaguya 지형 카메라(TC1/TC2)를 시도했다. 앞뒤로 기울어진 두 대를 실은
스테레오 전용 카메라라 이 실험의 조건과 기하가 같다. 두 가지 걸림돌이 있었다.

| 받을 수 있는 형태 | 문제 |
|---|---|
| USGS 가 지도 투영한 COG | **전역 DEM 으로 이미 정사보정**되어 있다 — 고도에 따른 어긋남이 지워졌다 |
| DARTS 의 원본 (level 2B0) | 어긋남은 살아 있으나 **푸시브룸 카메라 모델**(SPICE/ISIS)이 있어야 고도로 바꿀 수 있다 |

첫 번째는 처리 기록에 그대로 적혀 있다 — `spiceinit ... shape=user
model=Lunar_LRO_LOLAKaguya_DEMmerge_Global_512ppd_radius.cub`. 실제로 재 보니
남은 어긋남과 LOLA 고도의 상관이 0.13 이었고, 맞춘 기울기가 촬영 기하가 예측한
값의 3분의 1이었다. `tools/check_real_pair.py` 가 이 확인을 다시 돌린다.

두 번째는 카메라 모델 없이 특징점만으로 맞춰 보았으나 실패했다 — SIFT 특징점
8000개 중 통과한 것이 15개, 타일 위상 상관의 응답 중앙값이 0.057 이었다. 티코
안쪽 벽은 태양 고도가 8도라 그림자가 지배하고, 두 카메라가 반대쪽에서 보므로
한쪽에 보이는 사면이 다른 쪽에서는 그늘이다.

**달 궤도 스테레오는 거의 전부 푸시브룸이다.** 한 줄씩 쓸어 담으면서 자세가 계속
변하므로 두 장 사이를 행렬 하나로 적을 수 없다. 다른 푸시브룸 사진을 찾아도 같은
벽에 부딪힌다.

**아폴로 매핑 카메라는 프레임 카메라다.** 한 순간에 한 장을 통째로 찍으므로 두 장
사이가 필수행렬 하나로 정확히 기술되고, 이 파이프라인의 정렬과 삼각측량이 그대로
성립한다.

### 11-2. 촬영 조건 — 전부 아카이브에 적힌 값이다

| | 값 |
|---|---:|
| 사진 | AS15-M-1000 / AS15-M-1001 (1971-07-31, 24초 간격) |
| 초점거리 | 76.054 mm = 2701 px |
| 촬영 고도 | 100.9 km |
| 베이스라인 | 36.3 km (두 중심의 경도 차 1.33°) |
| 수렴각 | 20.4° |
| 지상 화소 | 37.3 m |
| 시차 1픽셀이 바꾸는 Z | 0.104 km |

**고도의 크기를 정답에서 가져오지 않는다.** 초점거리는 카메라 검정값이고
베이스라인은 두 프레임의 중심 좌표에서 나온다. 둘 다 아카이브에 적힌 값이다.

### 11-3. 스캔에 남은 왜곡을 사진만으로 걷어낸다

처음 복원한 고도에 **중앙이 솟은 사발**이 있었다. 화면 중심으로부터의 거리
제곱과 상관이 0.77 이었는데, 실제 지형이 화면 중심을 기준으로 방사 대칭일 이유가
없으므로 그것은 지형이 아니라 왜곡이다. 이 판본의 스캔이 필름 형식을 정확히 몇
화소에 담았는지 아카이브에 적혀 있지 않아서 생긴 것이다.

계수를 **대응점만으로** 찾았다. "사발이 사라지는 값" 을 기준으로 훑는다.
정답 고도는 쓰지 않는다.

| | 사발 세기 | 필수행렬 내점 | 자세 차이 |
|---|---:|---:|---:|
| 왜곡 보정 없음 | 0.772 | 95% | 1.18° |
| **k1 = -0.0256** | **0.002** | **98%** | **0.20°** |

### 11-4. 결과

| | 값 |
|---|---:|
| 맞춘 대응점 | 5,043 (필수행렬 내점 4,936) |
| 정렬 후 세로 잔차 | 0.57 px (중앙값) |
| 값이 나온 화소 | 65.6% |
| 카메라까지 거리 | **102.5 km** (아카이브의 촬영 고도 100.9 km) |
| 복원한 기복 | **2.74 km** |
| 같은 지역 LOLA 기복 | 2.72 km |
| 기복의 비 | **1.01 배** |

**두 가지가 서로를 확인한다.** 사진에서 잰 어긋남만으로 계산한 카메라까지의 거리가
아카이브에 적힌 촬영 고도와 1.6% 안에서 맞고, 복원한 기복이 레이저로 잰 기복과
1% 안에서 맞는다. 어느 쪽도 정답 고도를 보고 맞춘 값이 아니다.

`outputs/07_apollo.png` 에 두 장과 복원한 고도가 나란히 있다. 왼쪽의 매끄러운
마레가 낮고 오른쪽 산지가 높게 나온다.

**남은 것.** 복원 결과를 지도 위에 정확히 놓으려면 촬영 자세 전체가 필요하다.
지금은 기복의 크기만 견줬고 화소 단위로 오차를 재지는 않았다. 한 쌍만 해 본
것이기도 하다.

## 12. 한계

- **본 실험의 두 장은 여전히 렌더링이다.** 밝기는 달 광도함수를 거치고 센서
  잡음까지 얹으며 표면 무늬는 실제 사진에서 가져오지만(8절), 두 장 자체는
  만든 것이다. 실제로 찍힌 사진으로는 따로 확인했고(11절), **그쪽은 한 쌍만
  해 봤고 화소 단위 오차까지는 재지 못했다.**
- **신뢰도가 약하다.** 정답 없이 계산되는 세 신호 모두 무작위보다는 낫지만
  오라클과는 멀다(9-2절). 가장 나은 광도 잔차도 순위 상관 +0.139 다.
  "여기는 믿어도 된다" 를 화소 단위로 말할 수준이 아니다.
- **블록 크기를 정답 없이 못 고른다.** 교차 각도 불일치는 붕괴는 잡지만 완만한
  뭉개짐은 못 잡는다. 정답으로 고른 값보다 15% 나쁜 값을 고른다(10절).
- **"대응이 없다"를 말할 수 없다.** 매처는 탐색 구간 안에서 반드시 무언가를
  고른다. 좌우 일관성 검사가 그 일부를 잡지만, 다른 관문을 통과한 화소 중
  1.7% 를 더 걸러 내는 데 그친다.
- **값이 나온 픽셀이 67.9% 다.** 나머지는 대부분 크레이터 안쪽 그늘로, 두
  사진에 같은 무늬가 보이지 않는 곳이다. 광자 잡음이 어두운 곳에서 상대적으로
  크다는 점까지 넣었으므로, 이제 그늘의 어려움은 물리적으로 표현되어 있다.
- **가림(occlusion)을 다루지 않는다.** 광선이 앞쪽 봉우리를 지나쳐 뒤쪽 지면에
  닿아도 그대로 받는다. 시선이 지면에 거의 수직이라 지금 조건에서는 문제가
  없지만, 수렴각을 더 키우면 확인이 필요하다.
- **고도 모델이 0.118 km/화소다.** 레이저 궤적을 격자로 편 것이라 매끈해서, 더 높은
  해상도 모델(LRO NAC DTM)보다 잔무늬가 적다.
- **촬영 자세를 정답으로 안다.** 우리가 정한 값이라 오차가 없다. 실제 궤도
  영상이라면 자세도 추정해야 하고, 그 오차가 결과를 흔든다.

## 13. 개선점 — 아직 안 한 것

과제 요청내용 3번("변환된 3D 이미지의 시각적 결과를 분석하고 개선점을 도출")에
대한 답이다.

**① 실제 사진을 지도 위에 놓고 화소 단위로 채점한다.** 11절에서 실제로 찍힌 두
장으로 복원까지 했지만, 견준 것은 기복의 크기뿐이다. 화소마다 오차를 재려면 복원
결과를 지도 좌표에 놓아야 하고 그러려면 촬영 자세 전체가 필요하다. 한 쌍이 아니라
여러 쌍으로 늘리는 것도 남아 있다.

**② 신뢰도를 쓸 만한 수준으로 올린다.** 지금 가장 나은 신호(광도 잔차)도 순위
상관 +0.139 에 그친다. 비용 곡선의 1등/2등 비(peak ratio)가 표준적인
후보인데, OpenCV 의 StereoSGBM 은 비용 부피를 밖으로 내주지 않는다. 직접 ZNCC 를
다시 재서 얹거나 매처를 바꿔야 한다.

**③ 픽셀 락킹을 더 줄인다.** 가로 2배 업샘플로 정수 부근 쏠림을 49.3 → 35.9%
까지 낮췄지만 여전히 균일(20%)보다 높다. 4배는 오히려 나빠졌다 — 보간이 없는
정보를 만들지는 못한다. 비용 곡선에 대칭 보간을 다시 얹거나 WLS 후처리를 쓰면
더 줄일 수 있다고 알려져 있는데, OpenCV 기본 배포에는 그 모듈이 없다.

**④ 더 높은 해상도 고도 모델을 쓴다.** LOLA 0.118 km 는 레이저 궤적을 격자로 편
것이라 매끈하다. LRO NAC DTM(0.002~0.005 km/화소)을 쓰면 잔무늬가 많아져 정합이 쉬워질
것으로 보이지만, PDS 원본 포맷과 전용 도구 체계가 필요하다. **남은 빈 곳
(23.4%)이 대부분 그늘이므로, 해상도보다 조명 조건이 다른 시점을 더 모으는
쪽이 먼저다** — 다만 그것은 실제 궤도 영상이 있어야 한다.

## 14. 파일 구조

```
src/
  camera.py       핀홀 카메라, 자세, 투영·역투영
  terrain.py      고도 모델 로드·렌더링·촬영 기하·달 광도함수·센서 모델
  stereo.py       상대 자세, 정렬, 시차, 깊이 맵, 무늬 관문
                  좌우 일관성·광도 잔차·희소화 곡선 (정답 없는 신뢰도)
                  잡음 추정·자기상관 (영상에서 정하는 파라미터)
  pointcloud.py   포인트 클라우드 변환, z-buffer, PLY 입출력
  metrics.py      깊이 오차, Chamfer, F-score
tests/
  test_terrain.py         지형·렌더링·촬영 기하·전체 파이프라인
  test_stereo.py          삼각측량 공식·정렬 창·세로 경로
  test_realism.py         달 광도함수·센서 모델·정답 없는 신뢰도
tools/
  get_moon_dtm.py         고도 모델 한 구역 내려받기
  get_apollo_pair.py      실제로 찍힌 사진 두 장 내려받기 (아폴로 15호)
  check_real_pair.py      Kaguya 스테레오 쌍을 바로 쓸 수 있는지 확인
  save_test_report.py     테스트 실행 결과를 outputs/ 로 저장
  build_report.py         발표자료(4장) + 발표 대본 생성
run_3d_experiment.py      실험 실행기 (렌더링한 두 장)
run_apollo_stereo.py      실제로 찍힌 두 장으로 고도 복원
figures.py                결과 그림 생성
outputs/                  그림 6장, PLY, metrics.json, 로그, pytest 결과
data/moon/                고도 모델 (저장소에 포함하지 않음)
```

---

## 자료 출처

> LRO LOLA 전역 고도 모델 `Lunar_LRO_LOLA_Global_LDEM_118m_Mar2014`,
> USGS Astrogeology Science Center / NASA LRO LOLA 팀. 공개 자료.
> https://planetarymaps.usgs.gov/mosaic/

고도 모델은 저장소에 포함하지 않으며 `tools/get_moon_dtm.py` 로 필요한 구역만
내려받는다(약 2 MB).
